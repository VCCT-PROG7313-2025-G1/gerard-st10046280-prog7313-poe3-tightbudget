package com.example.tightbudget.base

import androidx.appcompat.app.AppCompatActivity

abstract class BaseActivity : AppCompatActivity() {
    // For now, this class is empty, but it can be extended in the future
}